import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#11047A",
    },
    secondary: {
      main: "#4318FF",
    },
    special: {
      main: "#34A853",
    }
  },
});

export default theme;